# 🧩 Contributing Guide
---

## 🚀 1. 기본 워크플로우

1️⃣ **이슈 생성**

* `Issues → New issue` 클릭
* 템플릿 선택 (`[Frontend] Feature`, `[Backend] Bug` 등)
* 제목 예시:

  ```
  [Frontend] 로그인 페이지 UI 구현
  [Backend] 회원가입 API 오류 수정
  ```
* 생성 시 자동으로 라벨(`area:*`, `type:*`)과 프로젝트(`AlgoTrack`)가 지정됩니다.

2️⃣ **브랜치 생성 (JetBrains 자동 생성 사용 가능)**

* 이슈를 IDE와 연결하면 JetBrains가 자동으로 브랜치를 만듭니다.
* 브랜치 이름 예시:

  ```bash
  issue/FRONT-123-login-ui
  issue/BACK-210-api-bug
  ```

3️⃣ **작업 및 푸시**

```bash
git add .
git commit -m "feat(frontend): 로그인 페이지 UI 추가 (#123)"
git push -u origin issue/FRONT-123-login-ui
```

4️⃣ **PR 생성**

* 이슈 브랜치에서 `dev` 브랜치로 Pull Request 생성
* 리뷰 및 CI 통과 후 dev로 머지

5️⃣ **배포 시점에만 main으로 머지**

* 모든 기능이 dev에 머지되고 QA 완료 시 main에 반영

```bash
git checkout main
git merge --no-ff dev
git tag v0.4.0
git push origin main --tags
```

---

## ⚙️ 2. 브랜치 전략

### 🌱 브랜치 흐름 요약

```
issue/FRONT-123-login-ui  →  dev  →  main (배포 시)
```

* **이슈 브랜치:** JetBrains에서 자동 생성 (각 작업 단위)
* **dev 브랜치:** 모든 기능 통합용, 항상 최신 유지
* **main 브랜치:** 배포 전용, QA 통과 후에만 병합

### 💡 예시 흐름

```
1. JetBrains에서 이슈 연결 → issue/FRONT-123-login-ui 자동 생성
2. 작업 후 커밋/푸시
3. dev로 PR 생성 → 리뷰 후 머지
4. 배포 준비 완료 시 dev → main 머지 + 태그 생성
```

---

## 🧩 3. 커밋 규칙

```
<type>(<scope>): <subject>

<body: 변경 이유/세부사항>

<footer: Closes #123, BREAKING CHANGE 등>
```

예시:

```
feat(frontend): 로그인 페이지 UI 추가

- Tailwind 스타일 적용
- 반응형 레이아웃 개선

Closes #42
```

* **type:** feat | fix | refactor | chore | docs | test | ci
* **scope:** frontend | backend | fullstack | infra | docs


---

## 🔗 4. 이슈 & PR 규칙

### 🪄 이슈 자동 설정

* 모든 이슈는 자동으로 **AlgoTrack 프로젝트**에 등록됩니다.
* 라벨(`area:*`, `type:*`, `status:*`) 자동 적용

### 🔗 PR 작성 규칙

* **PR 제목:** `#이슈번호 [영역] type: 제목`
* **PR 예시:** `#42 [Frontend] feat: 로그인 페이지 UI 추가`
* **PR 본문:** `PR 템플릿`
* **리뷰어:** 상대방
* **머지 조건:** 리뷰 1명 이상 승인 + CI 성공

---

## 🧰 5. 배포 플로우

1️⃣ `dev` 안정화 → QA 완료
2️⃣ `main`에 머지 (보호 브랜치)
3️⃣ 버전 태그 생성
4️⃣ 배포 기록 업데이트 (`CHANGELOG.md`)

```bash
git checkout main
git merge --no-ff dev
git tag v1.0.0
git push origin main --tags
git checkout dev
git merge main && git push
```

---

## 🤝 6. 요약

| 단계         | 설명                                     |
| ---------- | -------------------------------------- |
| 🧩 이슈 생성   | GitHub 템플릿으로 생성, JetBrains에서 브랜치 자동 생성 |
| 💻 개발      | 각 이슈 브랜치에서 작업 후 dev로 PR                |
| 🔍 리뷰 & 머지 | CI 통과 + 리뷰 후 dev에 머지                   |
| 🚀 배포      | dev 안정화 → main 머지 + 태그                 |


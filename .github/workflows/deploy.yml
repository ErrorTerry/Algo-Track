name: Deploy Backend to EC2

on:
  push:
    branches:
      - dev          # dev에 푸시/머지될 때 자동 실행
  workflow_dispatch: # 필요할 때 Actions 탭에서 수동 실행

jobs:
  deploy-backend:
    runs-on: ubuntu-latest

    env:
      BACKEND_DIR: backend
      REMOTE_BACKEND_DIR: /home/ubuntu/Algo-Track/backend

    steps:
      # 1) 레포 체크아웃
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2) JDK 설정
      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # 3) gradlew 실행 권한 부여
      - name: Grant execute permission for gradlew
        run: |
          cd $BACKEND_DIR
          chmod +x gradlew

      # 4) 백엔드 빌드
      - name: Build Spring Boot JAR
        run: |
          cd $BACKEND_DIR
          ./gradlew clean bootJar

      # 5) 빌드 결과 확인 (디버그용 로그)
      - name: List built JARs
        run: |
          echo "=== Local JARs ==="
          ls -al $BACKEND_DIR/build/libs || echo "build/libs 폴더가 없음"

      # 6) EC2로 JAR 업로드
      - name: Upload JAR to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          # GitHub Actions 쪽 경로: backend/build/libs/*.jar
          source: ${{ env.BACKEND_DIR }}/build/libs/*.jar
          # EC2 쪽 목표 폴더: /home/ubuntu/Algo-Track/backend/build/libs
          target: ${{ env.REMOTE_BACKEND_DIR }}/build/libs
          # backend / build / libs 세 단계 잘라내고 파일명만 두기
          strip_components: 3

      # 7) EC2에서 백엔드 재시작
      - name: Restart backend on EC2
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            set -e

            echo "🔄 백엔드 서비스 재시작 중..."
            sudo systemctl restart algotrack-backend.service

            echo "⏳ 서비스 상태 확인..."
            sudo systemctl status algotrack-backend.service --no-pager

            echo "🎉 재시작 완료!"
